Não é orientada a objetos

camelCase -> Na criação de variáveis

let -> O valor da variavel só é valido dentro de num bloco

    let x = 10;

    if {
        let = 2
    }

    // x continua a ser 2


Tipos de dados
    - String
    - Number
    - Boolean