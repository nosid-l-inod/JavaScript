// Operators in JavaScript

// Variables
let x = 10;
let y = 20;


// Addition
let sum = x + y;

// Subtraction
let subtraction = x - y;

// Multiplication
let multiplication = x * y;

// Division
let division = x / y;

// Strict equality (===) (Returns true or false)
// Check if two variables are equal and of the same data type
console.log(x === 10);

// Not (!)

// Does-not-equal (!==)
